[project]
name = "fastapi-start"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "daw159", email = "mdawood78612@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
fastapi-start = "fastapi_start:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
