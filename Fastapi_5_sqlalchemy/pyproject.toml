[project]
name = "fastapi-5-sqlalchemy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "daw159", email = "mdawood78612@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "sqlalchemy>=2.0.51",
]

[project.scripts]
fastapi-5-sqlalchemy = "fastapi_5_sqlalchemy:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
